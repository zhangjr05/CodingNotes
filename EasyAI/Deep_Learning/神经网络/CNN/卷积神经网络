import torch

# 卷积神经网络的基本结构

# 输入层    接受原始图像数据
# 卷积层:   用卷积核提取局部特征
# 激活函数: 应用非线性激活函数 如 ReLU
# 池化层:   降维 保留特征信息
# 归一化层: 加速训练过程 提高模型稳定性
# 全连接层: 综合特征 进行最终分类或回归
# 输出层:   不同形式
# 损失函数: 衡量模型预测与真实标签之间的差异 如 交叉熵损失(分类) 均方误差(回归)
# 优化器:   根据损失函数的梯度更新网络的权重 如 随机梯度下降 Adam RMSprop
# 正则化:   防止模型过拟合

