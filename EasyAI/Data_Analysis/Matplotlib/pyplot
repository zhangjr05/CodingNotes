import matplotlib.pyplot as plt
import numpy as np

xpoints = np.array([1, 2, 3, 4, 5])
ypoints = np.array([0, 100, 0, 100, 0])

# pyplot包的函数
plt.plot(xpoints, ypoints)  # 线图
plt.scatter()   # 散点图
plt.bar()   # 条形图
plt.hist()  # 直方图
plt.pie()   # 饼图
plt.imshow()    # 绘制图像
plt.subplots()  # 创建子图

plt.show()
